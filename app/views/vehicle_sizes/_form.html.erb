<%= form_with(model: vehicle_size, local: true) do |form| %>
  <% if vehicle_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle_size.errors.count, "error") %> prohibited this vehicle_size from being saved:</h2>

      <ul>
      <% vehicle_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vehicle_size_code %>
    <%= form.text_field :vehicle_size_code %>
  </div>

  <div class="field">
    <%= form.label :vehicle_size_name %>
    <%= form.text_field :vehicle_size_name %>
  </div>

  <div class="field">
    <%= form.label :vehicle_size_description %>
    <%= form.text_field :vehicle_size_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
