<p id="notice"><%= notice %></p>

<h1>Credit Notes</h1>

<table>
  <thead>
    <tr>
      <th>Credit note creation date</th>
      <th>Credit note informed gross amount</th>
      <th>Credit note sii status</th>
      <th>Receipt</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @credit_notes.each do |credit_note| %>
      <tr>
        <td><%= credit_note.credit_note_creation_date %></td>
        <td><%= credit_note.credit_note_informed_gross_amount %></td>
        <td><%= credit_note.credit_note_sii_status %></td>
        <td><%= credit_note.receipt %></td>
        <td><%= link_to 'Show', credit_note %></td>
        <td><%= link_to 'Edit', edit_credit_note_path(credit_note) %></td>
        <td><%= link_to 'Destroy', credit_note, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Credit Note', new_credit_note_path %>
