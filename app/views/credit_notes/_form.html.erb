<%= form_with(model: credit_note, local: true) do |form| %>
  <% if credit_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_note.errors.count, "error") %> prohibited this credit_note from being saved:</h2>

      <ul>
      <% credit_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :credit_note_creation_date %>
    <%= form.date_select :credit_note_creation_date %>
  </div>

  <div class="field">
    <%= form.label :credit_note_informed_gross_amount %>
    <%= form.number_field :credit_note_informed_gross_amount %>
  </div>

  <div class="field">
    <%= form.label :credit_note_sii_status %>
    <%= form.text_field :credit_note_sii_status %>
  </div>

  <div class="field">
    <%= form.label :receipt_id %>
    <%= form.text_field :receipt_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
